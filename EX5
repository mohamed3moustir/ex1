   #include <stdio.h>

int main() {
 
int M[5];
  int min=__INT_MAX__;
   for(int i=0;i<5;i++){
       printf("enter MIN");
       scanf("%d",&M[i]);
      if(min>M[i]) 
       min=M[i];
       
   }
   printf("MIN IS:%d \n",min);
 
 return 0
}
